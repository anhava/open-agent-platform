import { redirect } from 'next/navigation';
import KeystaticAdmin from '@aihio/keystatic/admin';

export default function Layout() {
  if (process.env.NODE_ENV === 'production') {
    redirect('/');
  }

  return <KeystaticAdmin />;
}